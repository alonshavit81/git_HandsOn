# git_HandsOn

`seqClass.py` classifies a nucleotide sequence as **DNA**, **RNA**, or **ambiguous** (could be DNA or RNA).  
It can also search for a **motif** inside the sequence.

## Requirements
- Python 3

## Usage

### 1) Classify a sequence
```bash
python seqClass.py -s ACTG
